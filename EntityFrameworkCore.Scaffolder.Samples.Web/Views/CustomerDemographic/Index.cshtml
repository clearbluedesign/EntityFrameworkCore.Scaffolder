@model IEnumerable<ClearBlueDesign.EntityFrameworkCore.Scaffolder.Samples.Web.Data.CustomerDemographic>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerDesc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomerTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomerTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomerTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
